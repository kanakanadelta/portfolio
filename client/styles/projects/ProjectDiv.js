import styled from 'styled-components';

export default styled.div`
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  align-items: center;

  .image {
    maxWidth: 600px;
  }

  .info {
    overflow: hidden;
    maxWidth: 600px;
  }
`